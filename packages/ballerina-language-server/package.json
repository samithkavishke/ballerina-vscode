{
  "name": "ballerina-language-server",
  "version": "0.0.0",
  "private": true,
  "description": "Ballerina Language Server (Gradle build wrapped for rush).",
  "scripts": {
    "build": "node ../../common/scripts/sync-version.js && ./gradlew build pack -x test",
    "clean": "./gradlew clean",
    "test": "./gradlew test"
  }
}
